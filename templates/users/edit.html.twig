{% extends 'base.html.twig' %}


{% block content %}

    <h2>User management <small>{{ user.id ? ('Edit'~ user.login) : 'New' }} user</h2>
    
    <div class="row">
        <div class="col-md-5">
            {{ form(form) }}
        </div>
    </div>

{% endblock %}