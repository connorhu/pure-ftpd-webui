{% extends 'base.html.twig' %}


{% block content %}

    <h2>Settings</h2>
    
    <div class="row">
        <div class="col-md-5">
            {{ form_start(form) }}
            <fieldset>
                <legend>WebUI Settings</legend>
                
                {{ form_row(form.FTPDUIDefaultDir) }}

                {{ form_row(form.FTPDUIDefaultGID) }}
                {{ form_row(form.FTPDUIDefaultUID) }}

                {# {{ form_row(form.FTPDUIDefaultUploadSpeed) }}
                {{ form_row(form.FTPDUIDefaultDownloadSpeed) }} #}
                
            </fieldset>
            {{ form_rest(form) }}
        </div>
    </div>

{% endblock %}